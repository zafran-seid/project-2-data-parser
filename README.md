#type-safe data parse 

#overview   this program validates data which comes from the user using typescript   
in this program  
    -data is type-safe 
    -invalid inputs are detected early
    -error is reproduced
json file include
{
 "users": [],
  "orders": []
}
